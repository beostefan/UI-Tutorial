import React from "react";

const Counter = props => {
    return (
        <div className="Counter">
            <b>Todo Count: </b>
            {/* TODO */}
        </div>
    );
};

export default Counter;
